---
title: "高速コミットタスク"
read_only: true
type: "command"
---

# 高速コミットタスクの作成

このタスクは、コミットタスク（`.claude/commands/commit.md`）と同じロジックを使用しますが、確認を求めずに最初に提案されたコミットメッセージを自動的に選択します。

- はじめに `git add -A` ですべてをステージングする
- ステージング後 `git diff --cached --name-only` で対象ファイルを表示する（意図しないファイルの混入防止）
- 意図しない変更が含まれる場合はコミット前にユーザーへ報告し、同一 `/commit!` 手順内で継続可否を確認する
- コミットタスクと同じフォーマットで3つのコミットメッセージを提案
- ユーザーに確認せず、最初の提案を自動的に使用
- 最初のメッセージで即座に `git commit -m` を実行
- その他の動作はすべてコミットタスクと同じ（フォーマット、scope判定、ステージされたファイルのみ）
- Claude共著フッターを追加しない
